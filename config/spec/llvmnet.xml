<?xml version="1.0"?>
<!DOCTYPE flagsdescription SYSTEM "http://www.spec.org/dtd/cpuflags2.dtd">
<flagsdescription>
  <filename>llvmnet</filename>
  <title>llvmnet LLVM to .NET CIL Toolchain Flags</title>
  <flag name="compiler_path" class="compiler" regexp="(?:/[^\s]+/)?llvmnet-(?:clang\+\+|clang|flang)(?=\s|$)">
    <![CDATA[<p>Runs the llvmnet C, C++, or Fortran compiler driver. Clang/Flang 22 emits LLVM bitcode, which is lowered to .NET CIL.</p>]]>
  </flag>
  <flag name="runtime_mode" class="portability" regexp="--runtime=(system|portable|managed-host)">
    <![CDATA[<p>Selects a runtime and ABI contract. System uses the Linux x86-64 ABI, glibc and native pthreads through generated ABI bridges. Portable uses the toolchain-owned LP64 dotnet64-v1 ABI and managed adapters. Managed-host is the legacy managed runtime with Linux host headers. Object modes cannot be mixed.</p>]]>
  </flag>
  <flag name="nativeaot" class="optimization" regexp="--nativeaot">
    <![CDATA[<p>Publishes the generated CIL executable using .NET 10 NativeAOT. The default NativeAOT optimization preference is Balanced. The generated executable embeds the .NET runtime and any generated native ABI call bridges; ordinary system-library dependencies remain.</p>]]>
  </flag>
  <flag name="optimization_level" class="optimization" regexp="-O[0-3sz]\b">
    <![CDATA[<p>Selects the Clang/Flang optimization level for LLVM generation. The SPEC development configuration defaults to O3. The driver defaults to disabling loop and SLP vectorization and C stack protectors because those lowering paths are not yet complete. LLVM global dead-code elimination is applied during executable linking.</p>]]>
  </flag>
  <flag name="vectorization" class="optimization" regexp="-f(?:slp-)?vectorize\b">
    <![CDATA[<p>Explicitly enables LLVM loop or SLP vectorization for a separate experimental validation run. This does not imply hardware SIMD lowering or full vector instruction support in the CIL backend.</p>]]>
  </flag>
  <flag name="cpp_standard" class="portability" regexp="-std=gnu\+\+(14|17|20|23)">
    <![CDATA[<p>Selects the GNU C++ language dialect. The harness wrapper defaults to C++14 for CPU 2017 and C++17 for CPU 2026.</p>]]>
  </flag>
  <flag name="SPEC_LP64" class="portability" regexp="-DSPEC_LP64\b">
    <![CDATA[<p>Selects the benchmark's LP64 data-model portability definitions.</p>]]>
  </flag>
</flagsdescription>